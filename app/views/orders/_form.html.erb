<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_id %>
    <!--<%= form.text_field :item_id, id: :order_item_id %>-->
     <%= form.collection_select(:item_id, Item.all, :id, :id) %>
  </div>

  <div class="field">
    <%= form.label :order_quantity %>
    <%= form.text_field :order_quantity, id: :order_order_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
